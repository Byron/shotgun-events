shotgun-events-engine:
  connection:
    retries: 1
    retry-every: 1s
  poll-every: 0s
  socket-timeout: 1s
  event-journal-file: $RW_DIR/journal.pickle
  logging:
    one-file-per-plugin: yes
    plugin-log-tree: $RW_DIR
    # path: $RW_DIR/sgevents.log
    # email:
    #   host: host.intern
    #   from: shotgun-events@server.intern
    #   subject: argh
    #   to:
    #     - noone@server.intern
    #     - someone@server.intern
    #   username: login
    #   password: pass

logging:
  verbosity: DEBUG

packages:
  shotgun-events:
    requires:
      - mock
    python:
      plugin_paths:
        - '{settings-files.test-site.dirname.dirname}/plugins'
  nosetests:
    requires:
      - shotgun-events